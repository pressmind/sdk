#!/usr/bin/env php
<?php
/**
 * Pressmind SDK File Downloader
 *
 * Downloads files with download_successful=0.
 * Requires bootstrap to be loaded (e.g. call from travelshop cli/file_downloader.php).
 *
 * Usage:
 *   php bin/file-downloader
 */

// Find autoloader - supports installation as vendor package and standalone
$autoloadPaths = [
    __DIR__ . '/../vendor/autoload.php',           // Standalone SDK
    __DIR__ . '/../../../autoload.php',            // Installed as vendor package
];

$autoloaderFound = false;
foreach ($autoloadPaths as $autoloadPath) {
    if (file_exists($autoloadPath)) {
        require_once $autoloadPath;
        $autoloaderFound = true;
        break;
    }
}

if (!$autoloaderFound) {
    fwrite(STDERR, "Autoloader not found. Please run 'composer install'.\n");
    exit(1);
}

$command = new \Pressmind\CLI\FileDownloaderCommand();
exit($command->run($argv));
